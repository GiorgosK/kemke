{#
/**
 * @file
 * Theme override to display the account menu with icons.
 */
#}
{{ attach_library('kemke/account-menu-icons') }}
{% if items %}
  <ul{{ attributes.addClass('menu govgr-nav__list kemke-account-menu') }}>
    {% for item in items %}
      {%
        set classes = [
          'menu-item',
          'govgr-nav__list-item',
          item.is_expanded ? 'menu-item--expanded',
          item.is_collapsed ? 'menu-item--collapsed',
          item.in_active_trail ? 'menu-item--active-trail',
        ]
      %}
      {% set item_path = item.url.toString %}
      {% set item_title = item.title|striptags|trim %}
      {% set icon_class = '' %}
      {% set open_in_new_tab = false %}
      {% set link_classes = ['govgr-nav__list-item-link', 'govgr-link', 'kemke-account-menu__link'] %}
      {% if item.in_active_trail %}
        {% set link_classes = link_classes|merge(['is-active']) %}
      {% endif %}
      {% if item_path starts with '/reports' %}
        {% set icon_class = 'kemke-menu-icon--report' %}
      {% elseif item_path starts with '/cases' %}
        {% set icon_class = 'kemke-menu-icon--case' %}
      {% elseif item_path starts with '/incoming' or item_path starts with '/amke' %}
        {% set icon_class = 'kemke-menu-icon--incoming' %}
      {% elseif item_path starts with '/contacts' %}
        {% set icon_class = 'kemke-menu-icon--contacts' %}
      {% elseif item_path starts with '/repository' %}
        {% set icon_class = 'kemke-menu-icon--repository' %}
      {% elseif item_path starts with '/user/login' %}
        {% set icon_class = 'kemke-menu-icon--login' %}
      {% elseif item_path starts with '/user/logout' %}
        {% set icon_class = 'kemke-menu-icon--logout' %}
      {% elseif item_path starts with '/manual' %}
        {% set icon_class = 'kemke-menu-icon--manual' %}
        {% set open_in_new_tab = true %}
      {% endif %}
      {% set link_attributes = {'class': link_classes, 'title': item.title} %}
      {% if open_in_new_tab %}
        {% set link_attributes = link_attributes|merge({'target': '_blank', 'rel': 'noopener noreferrer'}) %}
      {% endif %}
      <li{{ item.attributes.addClass(classes) }}>
        <a href="{{ item_path }}"{{ create_attribute(link_attributes) }}>
          <span class="kemke-menu-icon {{ icon_class }}" aria-hidden="true"></span>
          <span class="visually-hidden">{{ item.title }}</span>
        </a>
      </li>
    {% endfor %}
  </ul>
{% endif %}
